<Window x:Class="HowToWpfResearch.Src.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:HowToWpfResearch.Src.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800"
        d:DataContext="{d:DesignInstance vm:MainViewModel, IsDesignTimeCreatable=True}">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="{Binding Greeting}" FontSize="18" Margin="0 10"/>
        <Button Content="Click Me" Command="{Binding UpdateGreetingCommand}" Width="150"/>
    </StackPanel>
</Window>