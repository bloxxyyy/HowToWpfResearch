<UserControl x:Class="HowToWpfResearch.Src.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HowToWpfResearch.Src.ViewModels"
             xmlns:design="clr-namespace:HowToWpfResearch.Src.Design"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance design:DesignHomeViewModel, IsDesignTimeCreatable=True}">

    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="{Binding Greeting}" FontSize="18" Margin="0 10"/>
        <Button Content="Load Greeting" Command="{Binding LoadGreetingCommand}" Width="150"/>
    </StackPanel>
</UserControl>